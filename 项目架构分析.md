# Android 项目目录架构详细分析

## 📁 项目根目录结构

### 🔧 构建配置系统
```
/build.gradle.kts           # 项目级构建脚本
/settings.gradle.kts        # 项目设置和模块配置
/gradle.properties          # Gradle 全局属性配置
/gradle/                    # Gradle 包装器和版本管理
  ├── libs.versions.toml    # 版本目录 (Version Catalog)
  └── wrapper/              # Gradle 包装器文件
    ├── gradle-wrapper.jar  # Gradle 包装器 JAR
    └── gradle-wrapper.properties  # 包装器配置
/gradlew / gradlew.bat      # Gradle 执行脚本 (Linux/Mac / Windows)
/local.properties           # 本地环境配置 (SDK 路径等)
```

**关键特性：**
- 使用 **Kotlin DSL** (`.kts`) 替代传统的 Groovy 构建脚本
- **Version Catalog** 统一管理依赖版本
- **Gradle Wrapper** 确保构建环境一致性
- **AndroidX** 迁移支持和优化配置

---

## 📱 app 模块架构

### 🏗️ 模块构建配置
```
/app/
├── build.gradle.kts        # 模块级构建配置
├── proguard-rules.pro      # 代码混淆规则
├── build/                  # 构建输出目录
└── src/                    # 源代码目录
```

**构建配置特点：**
- **compileSdk 36**: 使用最新的 Android SDK
- **minSdk 24**: 支持 Android 7.0 (Nougat) 及以上
- **Jetpack Compose**: 启用 Compose UI 框架
- **Material 3**: 使用 Material Design 3 设计语言

---

## 💻 源代码目录结构 (`app/src/`)

### 📂 主要源代码 (`main/`)
```
main/
├── AndroidManifest.xml     # 应用清单文件
├── java/                   # Kotlin 源代码
│   └── com/example/myapplication/
│       ├── MainActivity.kt # 主活动类
│       └── ui/theme/       # UI 主题包
│           ├── Theme.kt    # Material 3 主题配置
│           ├── Color.kt    # 颜色定义
│           └── Type.kt     # 字体排版定义
└── res/                    # 资源文件目录
```

### 🎯 核心组件分析

#### 1. MainActivity.kt
```kotlin
class MainActivity : ComponentActivity() {
    // 使用 Jetpack Compose 构建 UI
    // 启用边到边显示 (enableEdgeToEdge)
    // 应用 Material 3 主题
}
```

#### 2. UI 主题系统
- **Theme.kt**: Material 3 主题配置，支持：
  - 动态颜色 (Android 12+)
  - 深色/浅色主题切换
  - 系统主题跟随
- **Color.kt**: 定义应用色彩方案
  - 亮色主题: Purple40, PurpleGrey40, Pink40
  - 暗色主题: Purple80, PurpleGrey80, Pink80
- **Type.kt**: 字体排版样式定义

---

## 🎨 资源文件组织 (`res/`)

### 📁 资源类型分布
```
res/
├── drawable/               # 可绘制资源
│   ├── ic_launcher_background.xml  # 启动器图标背景
│   └── ic_launcher_foreground.xml  # 启动器图标前景
├── mipmap-*/              # 多密度应用图标
│   ├── hdpi/              # 72×72
│   ├── mdpi/              # 48×48
│   ├── xhdpi/             # 96×96
│   ├── xxhdpi/            # 144×144
│   ├── xxxhdpi/           # 192×192
│   └── mipmap-anydpi-v26/ # 自适应图标
├── values/                # 值资源
│   ├── colors.xml         # 颜色资源 (当前为空)
│   ├── strings.xml        # 字符串资源
│   └── themes.xml         # XML 主题定义
└── xml/                   # XML 配置文件
    ├── backup_rules.xml   # 备份规则
    └── data_extraction_rules.xml  # 数据提取规则
```

### 📋 关键资源配置
- **应用名称**: "My Application"
- **主题**: Material Light NoActionBar
- **图标**: 自适应启动器图标
- **备份策略**: 完全备份支持

---

## 🧪 测试目录结构

### 📊 测试类型分离
```
test/                      # 本地单元测试
└── java/com/example/myapplication/
    └── ExampleUnitTest.kt
        ├── 测试逻辑: 2+2=4
        └── 执行环境: 开发机器 (JVM)

androidTest/               # 仪器化测试
└── java/com/example/myapplication/
    └── ExampleInstrumentedTest.kt
        ├── 测试内容: 应用上下文验证
        └── 执行环境: Android 设备/模拟器
```

### 🎯 测试框架
- **JUnit 4**: 单元测试框架
- **AndroidX Test**: Android 测试支持库
- **Espresso**: UI 测试框架 (已配置)

---

## 🏗️ 架构设计特点

### ✨ 现代开发特性
1. **Jetpack Compose**: 声明式 UI 框架
2. **Material 3**: 最新设计系统
3. **Kotlin**: 现代开发语言
4. **Gradle Kotlin DSL**: 类型安全构建脚本
5. **Version Catalog**: 依赖版本统一管理

### 🔄 模块化设计
- 单一 `app` 模块结构
- 清晰的包命名约定
- 分离的测试和源代码
- 资源文件类型化管理

### 🎨 设计规范
- Material Design 3 设计语言
- 响应式 UI 适配
- 深色模式支持
- 无障碍功能支持

---

## 📋 技术栈总结

| 技术领域 | 具体技术 | 版本 | 用途 |
|---------|---------|------|------|
| **开发语言** | Kotlin | 2.0.21 | 主要开发语言 |
| **UI 框架** | Jetpack Compose | 2024.09.00 | 声明式 UI |
| **设计系统** | Material 3 | - | 现代设计语言 |
| **构建工具** | Gradle | 8.13 | 项目构建 |
| **测试框架** | JUnit 4 | 4.13.2 | 单元测试 |
| **Android SDK** | compileSdk | 36 | 目标 SDK 版本 |

这个项目结构体现了 Android 开发的最佳实践，使用现代化的技术栈和规范的目录组织，为后续开发提供了良好的基础架构。